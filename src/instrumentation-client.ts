// MSW disabled — real Next.js API routes handle all requests and include audit logging
// if (process.env.NODE_ENV === "development") {
//   void import("@/mocks")
//     .then(({ initMocks }) => initMocks())
//     .catch((error) => {
//       console.error("[MSW] Failed to initialize mocks in instrumentation-client.", error);
//     });
// }
